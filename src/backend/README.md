# Backend

# Backend config
cd src/backend
python3 -m venv venv or similar, ensure it's called venv
source venv/bin/activate or similar
pip install -r requirements.txt

# Frontend config
cd src/frontend1
npm install
npm run start:dev

